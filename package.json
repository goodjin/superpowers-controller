{
  "name": "opencode-superpowers-controller",
  "version": "0.1.0",
  "description": "Unofficial stateful workflow controller for the Superpowers methodology on OpenCode.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "opencode-superpowers-controller": "./dist/cli/index.js"
  },
  "files": [
    "dist",
    "assets",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --external @opencode-ai/plugin --external @opencode-ai/plugin/tool && bun build src/cli/index.ts --outdir dist/cli --target bun --format esm --packages external && tsc --emitDeclarationOnly",
    "test": "bun test ./test/*.test.ts",
    "doctor": "bun run src/cli/index.ts doctor",
    "e2e:opencode": "bun run build && bun run scripts/e2e-opencode-1.16.2.ts"
  },
  "keywords": [
    "opencode",
    "superpowers",
    "workflow",
    "plugin",
    "agents"
  ],
  "license": "MIT",
  "dependencies": {
    "@opencode-ai/plugin": "^1.15.4",
    "jsonc-parser": "^3.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^6.0.0-dev.20260518"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  }
}
